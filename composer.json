{
    "name": "itpathsolutions/role-wise-session-manager",
    "description": "Role wise Managed session",
    "type": "library",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Itpathsolutions\\Sessionmanager\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Archana Soni",
            "email": "archanau@itpathsolutions.com"
        }
    ],
    "require": {
        "php": ">=7.0",
        "illuminate/support": ">=5.0 <12.0",
        "spatie/laravel-permission": ">=3.0 <7.0",
        "laravel/ui": ">=2.0 <5.0"
    },
    "extra": {
        "laravel": {
            "providers": [
                "Itpathsolutions\\Sessionmanager\\SessionManagerServiceProvider"
            ]
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (class_exists('Illuminate\\\\Support\\\\ServiceProvider')) { echo 'Illuminate Support is installed.\\n'; } else { echo 'Illuminate Support is missing. Please install it first.\\n'; exit(1); }\"",
            "@php -r \"if (class_exists('Spatie\\\\Permission\\\\PermissionServiceProvider')) { echo 'Spatie Permission is installed.\\n'; } else { echo 'Spatie Permission is missing. Installing now...\\n'; system('composer require spatie/laravel-permission'); }\"",
            "@php -r \"if (class_exists('Laravel\\\\Ui\\\\UiServiceProvider')) { echo 'Laravel UI is installed.\\n'; } else { echo 'Laravel UI is missing. Installing now...\\n'; system('composer require laravel/ui'); }\"",
            "@php artisan vendor:publish --provider=\"Itpathsolutions\\Sessionmanager\\SessionManagerServiceProvider\" --tag=config",
            "@php artisan migrate",
            "@php artisan ui:auth",
            "@php artisan migrate"
        ],
        "pre-uninstall-cmd": [
            "@php artisan package:remove-migration"
        ]
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "version": "1.0.5"
}
